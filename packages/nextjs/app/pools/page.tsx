import { PoolsPageContent } from "./_components/PoolsPageContent";

/**
 * POOLS PAGE (Server Component)
 * Browse and search all prediction pools
 * Protected - requires wallet connection
 */
export default function PoolsPage() {
  return <PoolsPageContent />;
}
