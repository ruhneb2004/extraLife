import { CreatePoolPageContent } from "./_components/CreatePoolPageContent";

/**
 * CREATE POOL PAGE (Server Component)
 * Create a new prediction pool
 * Protected - requires wallet connection
 */
export default function CreatePoolPage() {
  return <CreatePoolPageContent />;
}
